name: anandajk284238

on:
  workflow_dispatch:
    inputs:
      agent_name:
        description: 'finder'
        required: true
        default: 'finder'
      agent_description:
        description: 'finder'
        required: true
        default: 'finder AI Agent'

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      
